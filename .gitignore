# Compiled binary (root-level only — does NOT match cmd/server/ package)
/server
/app

# Environment — never commit real credentials
.env

# Claude Code local settings
.claude/

# Go build cache
*.test
*.out

# OS
.DS_Store
Thumbs.db
